import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FancyScreen = () => {
	return (
		<View>
			<Text>FancyScreen</Text>
		</View>
	);
};

export default FancyScreen;

const styles = StyleSheet.create({});
