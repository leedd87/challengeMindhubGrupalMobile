import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AdidasScreen = () => {
	return (
		<View>
			<Text>AdidasScreen</Text>
		</View>
	);
};

export default AdidasScreen;

const styles = StyleSheet.create({});
