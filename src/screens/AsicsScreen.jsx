import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AsicsScreen = () => {
	return (
		<View>
			<Text>AsicsScreen</Text>
		</View>
	);
};

export default AsicsScreen;

const styles = StyleSheet.create({});
